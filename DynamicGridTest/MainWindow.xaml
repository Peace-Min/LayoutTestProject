<Window x:Class="DynamicGridTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DynamicGridTest"
        mc:Ignorable="d"
        Title="WPF Dynamic Layout" Height="600" Width="800" Background="#1E1E1E" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="IconRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToggleButton">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#2D2D30" Padding="10" BorderBrush="#3E3E42" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Dynamic Chart Layout" Foreground="White" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- Chart Count ComboBox -->
                    <TextBlock Text="차트 개수:" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12"/>
                    <ComboBox x:Name="ChartCountCombo" Width="50" Margin="0,0,20,0" SelectedIndex="5" SelectionChanged="ChartCount_SelectionChanged"
                              Background="#333333" Foreground="White" BorderBrush="#555555" VerticalContentAlignment="Center" HorizontalContentAlignment="Center">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                        <ComboBoxItem Content="6"/>
                    </ComboBox>

                    <!-- Layout Options (Explicit XAML) -->
                    <TextBlock Text="레이아웃:" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12"/>
                    <StackPanel Orientation="Horizontal">
                        <!-- Landscape Grid -->
                        <RadioButton Tag="GridLandscape" Click="LayoutRadioButton_Click" ToolTip="가로형 그리드" Style="{StaticResource IconRadioButtonStyle}" IsChecked="True" Margin="0,0,4,0">
                            <Path Data="M4,4H10V10H4Z M14,4H20V10H14Z M4,14H10V20H4Z M14,14H20V20H14Z" 
                                  Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Stretch="Uniform" Width="14" Height="14"/>
                        </RadioButton>

                        <!-- Portrait Grid -->
                        <RadioButton Tag="GridPortrait" Click="LayoutRadioButton_Click" ToolTip="세로형 그리드" Style="{StaticResource IconRadioButtonStyle}" Margin="0,0,4,0">
                            <Path Data="M6,2H10V8H6Z M14,2H18V8H14Z M6,10H10V16H6Z M14,10H18V16H14Z" 
                                  Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Stretch="Uniform" Width="14" Height="14"/>
                        </RadioButton>

                        <!-- Vertical Stack -->
                        <RadioButton Tag="StackVertical" Click="LayoutRadioButton_Click" ToolTip="세로 스택" Style="{StaticResource IconRadioButtonStyle}" Margin="0,0,4,0">
                            <Path Data="M8,2H16V6H8Z M8,9H16V13H8Z M8,16H16V20H8Z" 
                                  Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Stretch="Uniform" Width="14" Height="14"/>
                        </RadioButton>

                        <!-- Horizontal Strip -->
                        <RadioButton Tag="StackHorizontal" Click="LayoutRadioButton_Click" ToolTip="가로 스트립" Style="{StaticResource IconRadioButtonStyle}" Margin="0,0,4,0">
                            <Path Data="M2,8H6V16H2Z M9,8H13V16H9Z M16,8H20V16H16Z" 
                                  Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Stretch="Uniform" Width="14" Height="14"/>
                        </RadioButton>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
             <UniformGrid x:Name="MainGrid" Rows="2" Columns="3" Margin="10"/>
        </ScrollViewer>
    </Grid>
</Window>
